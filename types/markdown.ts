/**
 * Props for the MarkdownRenderer component.
 */
export interface MarkdownRendererProps {
  /** The raw markdown string to render. */
  content: string;
  /** Optional additional CSS class names applied to the wrapper. */
  className?: string;
}
